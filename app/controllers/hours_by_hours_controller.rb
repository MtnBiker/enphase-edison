class HourByHoursController < ApplicationController
  
  private
    # Use callbacks to share common setup or constraints between actions.
    # def set_energy
    #   @energy = Energy.find(params[:id])
    # end

    # Only allow a list of trusted parameters through.
    # the following was suggested for date picking, but wasn't doing it 
  # def hour_by_hour_params
  #   params.require(:hour_by_hour).permit(:theDate, # other permitted attributes)
  # end
end
